dependencies {
    api project(':xcore')
    implementation 'redis.clients:jedis:3.10.0'
}

group = 'com.xwintop'
version = '0.0.1'
description = 'x-RedisTool'
//打jar包需要包含依赖的配置
jar {
    from { configurations.compileClasspath.findAll { it.name.contains("jedis") }.collect { zipTree(it) } }
}